name: shared_ui
description: UI components for the projects
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  path: ^1.8.2
  presentation: ^2.2.0
  flutter_animate: ^4.2.0
  provider: ^6.0.5

  shared_theme:
    path: ../shared_theme


flutter:
  uses-material-design: true

  assets:
    - assets/